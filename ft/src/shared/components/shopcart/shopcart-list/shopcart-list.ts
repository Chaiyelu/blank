import { Component, OnInit, OnChanges, AfterViewChecked, Input, Output, EventEmitter, ViewChild, ElementRef } from '@angular/core';
import { NavParams, ViewController } from 'ionic-angular';
@Component({
  selector: 'shopcart-list',
  templateUrl: 'shopcart-list.html'
})
export class ShopcartList{
  // choosedFoods:any[] = [];
  // constructor(
  //   public viewCtrl: ViewController,
  //   public navParams:NavParams
  // ) {


  // }

  // ngOnInit(){
  //   this.choosedFoods = this.navParams.get('data');
  // }

  // chooseFoods(){
  //   console.log(this.choosedFoods);
  // }

  // dismiss() {
  //   // let data = { 'foo': 'bar' };
  //   this.viewCtrl.dismiss();
  // }

}
